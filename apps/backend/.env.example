DATABASE_URL="postgresql://postgres:postgres@localhost:5432/smart_wms"

# Mật khẩu cho tài khoản seed (admin / staff01).
# BẮT BUỘC phải set khi chạy seed ở production (NODE_ENV=production) — script sẽ throw nếu thiếu.
# Ở local/dev, nếu không set sẽ dùng giá trị mặc định (Admin@123 / Staff@123) chỉ để tiện dev.
SEED_ADMIN_PASSWORD=""
SEED_STAFF_PASSWORD=""
